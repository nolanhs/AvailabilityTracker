name: Backend CI

on:
  workflow_dispatch:
  push:
    branches: [backend-devops]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo 'Backend CI is now registered'
